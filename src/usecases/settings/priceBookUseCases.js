const productRepository = new (require("../../repositories/orderRepository"))();

const getAllPriceBook = async () => {
};

const createPriceBook = async (status_name) => {
};

const deletePriceBook = async (id) => {
};

module.exports = {

};
