const productRepository = new (require("../../repositories/orderRepository"))();

const getAllProductCategory = async () => {
};

const createProductCategory = async (status_name) => {
};

const deleteProductCategory = async (id) => {
};

module.exports = {

};
